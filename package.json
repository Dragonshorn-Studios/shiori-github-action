{
  "name": "shiori-github-action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Export a bounded AFFiNE knowledge tree to deterministic repository-native Markdown.",
  "scripts": {
    "build": "ncc build src/main.js -o dist --minify",
    "test": "node --test",
    "check": "node --check src/main.js && node --check src/compiler.js && node --check src/affine-cli-source.js && node --check src/affine-properties.js && node --check src/icons.js && node --check src/skills.js"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1",
    "yjs": "^13.6.27"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
